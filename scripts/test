#!/usr/bin/env bash
set -euo pipefail

HOST="${1:-$(hostname)}"

echo "Building $HOST"
sudo nixos-rebuild build --flake ".#$HOST"

echo "Dry-run $HOST"
sudo nixos-rebuild dry-run --flake ".#$HOST"

echo "Done"
